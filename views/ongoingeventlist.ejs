<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style">
    <title>Ongoing Events</title>
</head>
<body>
    <!-- NavBarStart -->
    <header>
        <nav class="navContainer">
            <nav class="logo-title">
                <a href="/">
                    <img src="/image" alt="logo" class="logo" />
                    <h1 id="page-title">AcTrak</h1>
                </a>
            </nav>
            <a href="/main"><i class="bi bi-house-door-fill"></i>Home</a>
        </nav>
    </header>
    <hr>
    <!-- NavBarEnd -->   
    <hr>
    
    <%if(ongoingeventdetails.length!=0){%>
        <h1>Events in Progress...</h1>
    <% } else{%>
        <h1>No events in progress</h1>
    <% } %>
    
    <% for(let i=0; i < ongoingeventdetails.length; i++) { %>
        <h3 class="centerLink">Event <%= i+1%> </h3>
        <div class="event-box", id="customers">
            <table>
                <tr>
                    <th>title</th>
                    <th><%=ongoingeventdetails[i]["_doc"]["title"]%> </th>
                </tr>
                <% for (const det in ongoingeventdetails[i]["_doc"]) {%>  
                    <%if(det!="__v" & det!="_id" & det!="title"){%> 
                    <tr>
                        <td id="event-details-attributes"><%=det%> </td>
                        <td> <%=ongoingeventdetails[i]["_doc"][det]%> </td>
                    </tr>
                    <%}%>
                <%}%>
            </table>
            <hr>
        </div>
    <% } %>
</body>
</html>





